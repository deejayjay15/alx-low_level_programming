#include "main.h"
/**
 * add - takes two integer values adds them and returns the result
 * @x: Integer for addition
 * @y: Integer for addition
 * Return: z, the value of x and y
 */

int add(int x, int y)
{
	int z;

	z = x + y;

	return (z);
}
